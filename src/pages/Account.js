import HomeCard from "../components/home/HomeCard";
import {cards} from "./../helpers/homeCardsList";

const Home = () => {
  return (
    <main className="landing-body">
      <article className="main-section">
        <h1 className="landing-title">Мій профіль</h1>
      </article>
    </main>
  );
};

export default Home;
